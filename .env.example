# Copy this file to .env and fill in your values
# NEVER commit your real .env file to GitHub!

# MongoDB Connection String
# Get this from MongoDB Atlas (free tier):
# 1. Go to https://cloud.mongodb.com
# 2. Create a free account
# 3. Create a free cluster (M0 - Free Forever)
# 4. Click "Connect" → "Drivers" → copy the connection string
# 5. Replace <password> with your database user password
MONGODB_URI=mongodb+srv://your_username:your_password@cluster0.xxxxx.mongodb.net/aish_health?retryWrites=true&w=majority

# Port (Render will set this automatically, don't change)
PORT=3000
